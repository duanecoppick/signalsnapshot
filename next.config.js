/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath removed - deploy at root for signalsnapshot.vercel.app
  // For askmiloai.com/signalsnapshot, use rewrites in the main domain's next.config.js
}

module.exports = nextConfig
